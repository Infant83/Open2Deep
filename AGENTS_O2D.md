# Project Open2Deep Instructions

- Prefer the dedicated OpenProject tools when the task touches OpenProject state.
- Keep results compact and ID-driven.
- Before creating, editing, or commenting on external systems, confirm the exact target unless the user has already been explicit.
- Use local `.o2d/`, project `skills/`, and `custom_tools/` content before inventing new one-off workflows.
